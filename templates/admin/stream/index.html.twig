{% extends 'base.html.twig' %}

{% block body_id 'stream_index' %}


{% block main %}
    <table class="table table-dark">
        <thead>
        <tr>
            <th scope="col">Site de streaming</th>
        </tr>
        </thead>
        {% for serie in liste %}
            <article class="post">
                {#<h2>#}
                {#<a href="{{ path('blog_post', {slug: post.slug}) }}">#}
                {#{{ post.title }}#}
                {#<h1>Robert</h1>#}
                {#</a>#}
                {#</h2>#}

                {#<p class="post-metadata">#}
                {#<span class="metadata"><i class="fa fa-calendar"></i> {{ post.publishedAt|localizeddate('long', 'medium', null, 'UTC') }}</span>#}
                {#<span class="metadata"><i class="fa fa-user"></i> {{ post.author.fullName }}</span>#}
                {#</p>#}



                <tr>

                    <td>{{ serie.nomSite }}</td>
                    <td class="text-right">
                        <div class="item-actions">
                            <a href="{{ path('stream_edit', {id: serie.id}) }}" class="btn btn-sm btn-primary">
                                <i class="fa fa-edit" aria-hidden="true"></i> {{ 'action.edit'|trans }}
                            </a>
                        </div>
                    </td>

                </tr>



            </article>
        {% else %}
            <div class="well">Aucun site trouvé</div>
        {% endfor %}
    </table>

    {#{% if series.haveToPaginate %}#}
    {#<div class="navigation text-center">#}
    {#{{ pagerfanta(series, 'twitter_bootstrap3_translated', {routeName: 'serie_index_paginated', routeParams: app.request.query.all}) }}#}
    {#</div>#}
    {#{% endif %}#}
{% endblock %}

{% block sidebar %}
    {{ parent() }}

    {{ show_source_code(_self) }}

{% endblock %}
